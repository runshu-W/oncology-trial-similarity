% =====================================================================
%  Supplementary Material for
%  "Retrospective predictive calibration of oncology trial similarity
%   for Bayesian historical borrowing"
%
%  Standalone document. Compile with:
%     latexmk -pdf supplement.tex
%  Updated 2026-08-31 for the selective-borrowing revision: the
%  Claim--Evidence Map matches the EB-referenced evaluation and the
%  selective two-head architecture; Section S2 collects the precise
%  configuration constants requested in pre-submission review.
% =====================================================================
\documentclass[11pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage[hidelinks]{hyperref}
\setlength{\parskip}{0.4em}
\setlength{\parindent}{0pt}


\title{Supplementary Material:\\
Retrospective predictive calibration of oncology trial similarity
for Bayesian historical borrowing}
\author{}
\date{}

\begin{document}
\maketitle

\renewcommand{\thesection}{S\arabic{section}}
\renewcommand{\thetable}{S\arabic{table}}

\section{Claim--Evidence Map}

This table maps each principal claim in the main text to its supporting
evidence and the boundary of that evidence. It is a cross-check aid: every
``Status'' entry states the setting in which the claim is supported and does
not assert clinical or regulatory validity. All real-data values correspond to
the unit-corrected data; ``test'' is the held-out $20\%$ split ($n=281$;
out-of-fit but not out-of-development, see the provenance paragraph of the
main text), ``forward'' the pooled disjoint future windows ($n=775$,
past-only refits). Design-time and outcome-adaptive comparisons are never
mixed.

\begin{longtable}{p{0.32\textwidth} p{0.39\textwidth} p{0.19\textwidth}}
\toprule
\textbf{Claim} & \textbf{Evidence} & \textbf{Status} \\
\midrule
\endhead
Flat-referenced borrowing gains are largely marginal-distribution information
in disguise. & Intercept-only EB (2 parameters, no candidates): test NLL
$2.9012$ vs weak $3.1641$; every fixed-budget borrowing construction at or
above EB (rule $3.0542$, paired $\Delta$ $+0.153$ CI $[+0.073,+0.237]$; best
robust-MAP $3.0427$). & Supported (retrospective ORR). \\
The selective architecture significantly improves on fixed-budget borrowing.
& Paired vs predecessor: test $-0.0581$ CI $[-0.1170,-0.0026]$; forward
$-0.0665$ CI $[-0.0993,-0.0346]$; stable across five seeds ($-0.058$ to
$-0.075$) and under donor-block resampling ($[-0.105,-0.030]$). & Supported
(test and forward). \\
The architectural gain is an interaction, not a sum of parts. & $2\times2$
factorial, pooled forward vs EB: flat/fixed $+0.0399$; EB anchor alone
$+0.0002$; learnable $\lambda_0$ alone $+0.0413$; combination $-0.0266$. &
Supported; attribution to ``selectivity alone'' not claimed. \\
Single-fit selective priors consistently but not significantly lead the EB
reference. & Test $\Delta$ $-0.0295$ CI $[-0.0722,+0.0109]$; forward
$-0.0266$ CI $[-0.0591,+0.0064]$; negative under all five seeds (forward
$-0.018$ to $-0.035$). & Supported as stated. \\
The pre-registered five-seed ensemble exceeds the EB reference on pooled
forward validation. & Ensemble NLL $2.8084$ vs $2.8440$; $\Delta$ $-0.0356$
CI $[-0.0670,-0.0059]$ (single pre-specified aggregate test); test-split
ensemble still inconclusive ($-0.0336$ CI $[-0.0755,+0.0070]$). & Supported;
lever pre-registered before execution, outcome-blind. \\
The most-recent-window lead is real and consistent with a learning curve. &
Window-3 ensemble $\Delta$ $-0.0480$, Bonferroni $98.33\%$ CI
$[-0.0947,-0.0024]$; composition-controlled comparison on the same 390
queries: 1{,}017-example model beats 813-example model by $-0.0236$ CI
$[-0.0424,-0.0057]$. & Supported; window-level residual confounding noted. \\
The learned total borrowing ranks realized benefit on unseen data. &
Spearman(mass, $\mathrm{NLL}_{EB}-\mathrm{NLL}_{sel}$) $= +0.16$ CI
$[+0.08,+0.24]$, $+0.16$ in each window; hand rule $+0.14$. & Supported
(modest, stable). \\
The triage's demonstrated per-query value is harm avoidance. & Lowest mass
tertile: predecessor $+0.148$ vs EB, selective $+0.003$ CI $[-0.007,+0.012]$;
middle tertile $-0.054$ CI $[-0.089,-0.019]$; top tertile $-0.029$ CI
$[-0.117,+0.060]$. & Supported. \\
Under strict registry availability, the aggregate advantage is not yet
realisable. & Donor results posted before query start: $4.2\%$ of gated
observations survive; availability-consistent rerun pooled $\Delta$
$-0.001$ CI $[-0.017,+0.016]$; available-donor subgroup ($n=179$) $-0.003$
CI $[-0.072,+0.072]$; availability $2\%\to31\%$ across windows. & Supported;
scopes all real-data claims. \\
Shared-donor dependence does not drive the conclusions. & $12.7\%$ of query
pairs share a gated donor; donor-block bootstrap CIs match query-level;
leave-one-donor-out pooled deltas $-0.018$ to $-0.037$. & Supported;
connected-components clustering degenerate and reported as such. \\
The advantage concentrates in percentage-derived, recent, larger trials. &
Count-reported query outcomes only: $-0.003$ CI $[-0.049,+0.047]$; dropping
percentage-derived donor observations: $-0.001$ CI $[-0.022,+0.020]$;
percentage-outcome group $-0.050$ (median year 2023). & Supported; the
completed audit vindicates the percentage stratum ($42/42$ denominators). \\
Selective calibration approaches the EB reference at lower NLL. & Test PIT-KS
$0.094$ (EB $0.035$, fixed-budget $0.173$); coverage $0.683/0.925/0.975$ at
nominal $0.50/0.80/0.95$. & Supported (held-out split). \\
Under exchangeability the selective prior gives the best type-I-controlled
power in the comparison. & Simulation, no conflict: type I $0.024$, power
$0.613$ (predecessor $0.588$; no borrowing $0.536$; EB-only $0.497$). &
Supported (simulation). \\
Standalone selective borrowing is unsafe under uniform whole-set conflict;
the anchored SAM companion is required. & Type I $0.056/0.145/0.243$ at
shifts $0.5/1.0/1.5$ (predecessor $\le 0.072$); fixed-discount ablation
$0.079$ at shift $1.0$; selective+SAM $0.041/0.070/0.046$ with power
$0.594$ at no conflict. & Supported (simulation); central safety finding. \\
In the selective architecture the per-source discount response is flat;
adaptivity migrates to the set level. & Borrowed size by true distance:
selective $43.6/43.7/43.8/43.9$ (flat) vs fixed-budget-with-signal
$31.3/30.4/28.0/23.6$ (declining). & Supported (simulation); reported as a
mechanistic regression. \\
Design-time signals of the kind used here cannot detect uniform whole-set
conflict. & S4 primary-label ROC-AUC $0.43$--$0.45$ (all design-time methods
below chance); outcome-dependent UIP-JS $0.813$ ($0.839$ process-compatible
labels). & Supported (simulation; structural). \\
In the external-control world the selective prior learns to withhold
borrowing. & Nominal borrowed size $\approx 0$ at every drift; OCs coincide
with EB-only (type I $\le 0.019$); power $0.631$ vs internal-only $0.237$
comes from the (there highly informative) EC anchor
$\mathrm{Beta}(19.0,78.7)$. & Supported (simulation; anchor caveat stated). \\
SAM layers are outcome-adaptive and scored separately, against each prior's
own weak component. & OA rows recomputed under the released adapter with
documented semantics: selective\_sam test $2.7995$ (KS $0.094$), two\_head\_sam
$2.8154$, rule\_sam $2.9226$. & Supported; descriptive only. \\
Numeric extraction integrity in the admitted subset is high, and the
percentage-reconstruction path is vindicated. & Expert audit, 80 stratified
rows, six items vs the live registry: arm $98.8\%$, numerator $97.5\%$,
denominator $97.5\%$, population $97.5\%$; all four jointly $93.8\%$ CI
$[86.2,97.3]$; percentage stratum denominators $42/42$. & Supported
(completed expert audit). \\
The two numeric errors found do not move the conclusions. & Proportion-row
double misparse (one observation, one query) and registry-version drift on
the most-shared donor ($4/12$ snapshot vs $4/13$ current, 307 examples);
corrected sensitivity: test $-0.0295 \to -0.0298$, forward $-0.0266 \to
-0.0273$. & Supported; guard added for the misparse signature. \\
About one in five admitted ``ORR'' rows is a neighbouring binary response
endpoint, the main residual data-quality limitation. & ORR-definition
agreement $77.5\%$ CI $[67.2,85.3]$ (count-reported $60.7\%$,
percentage-reported $83.3\%$); CR-only, CBR, pCR, composite remission,
molecular response, one non-tumour endpoint. & Supported; endpoint
canonicalisation identified as the fix; about $44\%$ of raw rows remain
excluded by the admission rule (synthetic round-trip $8/8$). \\
The endpoint-unit defect is quantified and corrected, and results were
regenerated. & $804/3{,}319$ arm-level ORR rows provably wrong pre-correction;
corpus $= 7{,}173$ trials, $26{,}603$ primary-endpoint arm rows;
$1{,}470 \to 1{,}407$ examples; full re-run with fixed seeds. & Supported. \\
Every partition is out-of-fit but none is out-of-development. & Provenance
paragraph (Section 2.11) discloses what each split was exposed to at each
stage; forward windows carry confirmatory weight; prospective registry
vintages identified as the true lockbox. & Supported (disclosure). \\
Reported learned results use genuine, freshly trained checkpoints, and the
round-2 machinery reproduces the published numbers before extending them. &
Selective replication drift $\le 1.4\times10^{-5}$; fixed-budget recompute
drift $\le 4\times10^{-13}$; simulation datasets bit-identical to the
released run, worst published-cell drift $0.0015$. & Supported. \\
\bottomrule
\end{longtable}

\section{Configuration constants and reproducibility details}

\paragraph{Candidate features (9 dimensions).} As defined in the main text:
$s_i$ = Stage-2 overall similarity $/100$; disease, regimen, endpoint,
follow-up, and eligibility match scores = the corresponding Stage-2 $0$--$5$
dimension scores $/5$; result-quality score $/5$; negative red-flag severity
$-\mathrm{rf}_i \in [-1,0]$; and $\log(1+n_i)$. Stage-2 dimension scores are
produced by the deterministic reranker released with the code
(\texttt{pipeline/\allowbreak oncology\_trial\_similarity\_pipeline.py}); the scoring
rules, keyword tables, and gate conditions are implemented there verbatim and
exercised by the released unit tests.

\paragraph{Gate and rule prior.} The conservative gate multiplies factors for
endpoint compatibility ($<1.5/5$ blocks), result usability ($0$ blocks),
disease match ($<1.5/5$ scales by $0.2$; $<2.5/5$ by $0.6$), and low-severity
red flags (scale $0.5$). Rule discounts $a^{\mathrm{rule}}_i$ follow the
suitability ladder $0.75/0.40/0.15/0$; the rule prior fixes $\lambda_0=0.2$
with a flat $\mathrm{Beta}(1,1)$ anchor.

\paragraph{Selective model.} DeepSets encoder $\phi$: $9 \to 16 \to 16$
(ReLU); score head $\rho$ and discount head: $32 \to 16 \to 1$; weak-component
head $\psi$: $16 \to 16 \to 1$ on the pooled context. Training: Adam, learning
rate $0.01$, $100$ epochs, full batch, seed $20260603$ (primary and every
window refit); KL-to-rule weight $\rho = 0.1$ on normalised candidate
allocation; listwise term disabled ($\eta = 0$); borrowed-information penalty
$10^{-4}\max(\sum_i \lambda_i a_i n_i - 100, 0)^2$. EB anchors by
beta-binomial MLE (grid plus coordinate refinement): training split
$\mathrm{Beta}(0.68, 1.70)$; past-only fits $\mathrm{Beta}(0.65,1.61)$,
$(0.68,1.70)$, $(0.69,1.72)$ at the 2020/2021/2022-12-31 cutoffs.
Disease-stratified EB: per-primary-site MLE for strata with $\ge 40$ training
queries, global fallback otherwise.

\paragraph{Evaluation constants.} Held-out split: deterministic $80/20$
(seed $20260603$), $n = 281$. Disjoint forward windows by primary completion
date: (2020-12-31, 2021-12-31] $n=181$; (2021-12-31, 2022-12-31] $n=204$;
after 2022-12-31, $n=390$; past-only training pools $632/813/1{,}017$.
Paired bootstrap: $4{,}000$ resamples, percentile intervals. PIT: randomized
form, RNG seed $20260707$; prediction intervals: equal-tailed on the exact
beta-binomial mixture pmf. Pre-specified information subgroup: $I = \sum_i
\mathrm{gate}_i a^{\mathrm{rule}}_i n_i \ge 8$, fixed from exploratory
analysis of the two earlier windows before the final window was examined.
Robust-MAP baseline: DerSimonian--Laird heterogeneity on the rate scale,
Haldane-smoothed rates, MAP ESS cap $200$, $\epsilon = 10^{-4}$,
robustification weights $w \in \{0.5, 0.9\}$. SAM adapter: temperature
$\tau = 1$; multiplier $\min(1, R^{\tau})$ with $R$ the
historical-to-weak predictive ratio at the observed outcome.

\paragraph{Gold-standard simulation.} As released in
\texttt{simulation/gold\_standard/}: $2{,}000$ replicates per cell (estimation
and design worlds), simulation training set $300$ queries, $60$ epochs, seed
$20260720$; design test $H_0: \theta \le 0.20$ at one-sided $0.025$,
alternative $\theta_0 = 0.35$; conflict grid $0$--$1.5$ logit shift; success
threshold $0.975$; fixed-discount comparator $0.35$. Data-generating
constants, trap/hidden-gem composition, and per-cell outputs are archived in
\texttt{results/tables/\allowbreak gold\_standard\_*}.

\paragraph{Round-2 additions (this revision).} Donor-availability analysis:
availability dates are ClinicalTrials.gov results-first-posted dates for all
$7{,}173$ corpus trials; the analysis-time rule restricts training queries and
their donors to results posted on or before each fold cutoff (pools
$323/494/651$; surviving donor observations $26.7\%/35.6\%/45.0\%$; refit EB
anchors $\mathrm{Beta}(0.68,1.57)$, $(0.66,1.58)$, $(0.68,1.66)$), and the
design-time scoring rule restricts each query's donors to results posted
before that query's start date; availability-filtered selective refits use
the canonical hyperparameters and seed. Dependence analysis: donor-block
bootstrap with blocks defined by each query's strongest gated donor ($346$
blocks over the $775$ forward queries, largest $36$; $4{,}000$ resamples,
seed $20260821$); connected components of the sharing graph are reported as
degenerate (one component holds $98\%$ of queries). Count-only sensitivity:
percentage-derived means the reported unit string contains
``percent(age)''; $3{,}678$ of $6{,}813$ donor observations and $683$ of
$1{,}407$ query outcomes are percentage-derived. Simulation round 2: primary
gold-standard labels use $|\theta_k - \theta_0| \le 0.05$ on latent rates
(secondary process-compatible labels use $\theta^{\mathrm{obs}}_k$); the
same per-cell dataset seeds as the released run (draws verified
bit-identical); selective simulation models trained on the same
$300$-query draw ($60$ epochs, seed $20260603$); simulation EB anchors
$\mathrm{Beta}(1.137, 2.103)$ (gold) and $\mathrm{Beta}(19.05, 78.66)$ (EC,
reflecting that world's homogeneous control rates); SAM for selective priors
is anchored (ratio and returned mass against the prior's own EB weak
component). Outcome-adaptive rows in the main tables were recomputed under
the released adapter implementation with each prior's own weak component
(randomized PIT seed $20260707$); the manual-audit sampling frame
(\texttt{docs/\allowbreak manual\_audit\_sample\_2026-08-31.csv}) oversamples
percentage-derived and most-shared donors (seed $20260822$); the completed
audit (\texttt{docs/\allowbreak manual\_audit\_sample\_2026-08-31\_completed.csv})
is analysed with Wilson $95\%$ intervals, and the audit-corrected
sensitivity rescoring (fix NCT03290079 $\to 2/18$, NCT03331250 $\to 4/13$;
drop two flagged $100\%$-proportion rows; 221 scorings touched, rescored
with the frozen canonical models, bootstrap seed $20260823$) is archived in
\texttt{artifacts/\allowbreak round2/audit\_sensitivity\_meta.json}.

\paragraph{Sensitivity artifacts.} Repository artifacts include section-weight
sensitivity (\texttt{results/tables/\allowbreak section\_weight\_sensitivity.csv}),
feature ablation, $\lambda_0$-frontier and conflict sweeps from the
operating-characteristics engine, and the per-example evaluation rows behind
every table in this revision
(\texttt{artifacts/\allowbreak rerun\_unitfix\_2026-08-14/} and the selective-revision
artifact set); regeneration commands are given in
\texttt{docs/reproducibility.md} and the figure scripts assert every plotted
headline value against the manuscript numbers before writing output.

\end{document}
